package genpay;

/*
 * @DataJpaTest
 *
 * @AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
 * class AgentRepositoryTest {
 *
 * @Autowired AgentRepository agentRepository;
 *
 * @Test
 *
 * @Tag("fast") // Categorizes this test as "fast"
 *
 * @DisplayName("Test if the injection is working") void shouldBeanSetted() {
 * assert (agentRepository != null); }
 *
 * //@Test void shouldDataBaseBeenPopulated() { assert
 * (agentRepository.findAllBy().getFirst().name() == "John"); }
 *
 * @Test
 *
 * @Tag("fast") // Categorizes this test as "fast"
 *
 * @DisplayName("Test if the database is populated") void
 * shouldDataBaseBeenEmpty() { assert (agentRepository.findAllBy().isEmpty() ==
 * true); } }
 */
